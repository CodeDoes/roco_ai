# v1 Milestone Checklist

> Single source of truth for the v1 release. Check off items as they land. Append to `roadmap/progress.md` when done.

---

## A. Current Phase: Desktop Experience Completion (Phase 2 → 3)

**Goal:** Wire the standalone-tested desktop widgets (`crates/ui/`) through `AppContext` so the full writer loop works: outline → chapter → feedback → quality → revise → publish.

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | All 8 widgets have `#[cfg(test)]` modules | ✅ | 101 tests pass |
| 2 | `RocoDesktopApp` wires all 6 browser panels | ✅ | FileTree, Wiki, LinkGraph, Sessions, Timeline, Editor |
| 3 | `desktop_app.rs` uses `AppContext` for workspace/session ops | ⏳ | Currently uses `ModelBackend` directly for chat; `AppContext` wired for sessions |
| 4 | Pacing widget → `InteractionState` → plan-first loop (`classify`/`derive`/`dispatch`) | ❌ | Phase 3.2 |
| 5 | Quality results surfaced in right panel (editor diff view) | ❌ | Phase 3.3 |
| 6 | Revision diff in markdown editor (accept section / accept selection) | ❌ | Phase 3.4 |
| 7 | End-to-end desktop test (`tests/desktop_e2e.rs`) | ❌ | Currently `.skip`; Phase 3.5 |

---

## B. CLI Parity

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | `start.sh` launches `story_human` example | ✅ | Auto-resolves local `.st` model |
| 2 | `roco interact --interactive` mirrors GUI pacing controls | ✅ | `/accept`, `/skip`, `/stop`, `/undo`, `/pace` |
| 3 | `roco story` runs full pipeline non-interactively | ❌ | `cmd_story` in `roco.rs` is a stub |
| 4 | `roco eval` / `roco bless` snapshot workflow | ✅ | Works against oracles |

---

## C. Model & Inference (Frozen)

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | RWKV-7 2.9B loads on Vulkan (release build) | ✅ | Debug hangs on WGPU validation |
| 2 | BNF-constrained decoding via `bnf-engine` + `kbnf` | ✅ | 6 GBNF grammars embedded |
| 3 | `feed_eos` (token 0) for recurrent state hygiene | ✅ | Verified by `token0_probe.rs` |
| 4 | State save/load/mix for workspace timeline | ✅ | Trait methods implemented |

---

## D. Editor Plugins (Thin Clients)

| # | Task | Status | Notes |
|---|------|--------|-------|
| 1 | VSCode plugin talks to `roco server` HTTP API | ✅ | `apps/plugins/vscode/` |
| 2 | Zed plugin talks to `roco server` HTTP API | ✅ | `apps/plugins/zed/` |
| 3 | Obsidian plugin talks to `roco server` HTTP API | ✅ | `apps/plugins/obsidian/` |

---

## E. v1 Release Criteria (Definition of Done)

| Criterion | Met? |
|-----------|------|
| `cargo test --workspace` passes (400+ tests) | ✅ |
| `cargo clippy --workspace --all-targets -- --deny warnings` clean | ⚠️ 44 unused-fn warnings in CLI (non-blocking) |
| Desktop GUI: human can write a 3-chapter story with feedback loop | ❌ |
| CLI `start.sh`: human can write a 3-chapter story with feedback loop | ✅ |
| Plugins: install, connect to local server, send prompt, get completion | ✅ |
| No frozen engine files modified since 2026-07-19 | ✅ |
| All docs reflect actual codebase (no dead links, no `apps/chat` refs) | ✅ |

---

## F. Post-v1 (Icebox)

- Epub / PDF export from desktop
- Multi-model routing (local + remote)
- Collaborative editing (OT/CRDT)
- Mobile companion app
- Fine-tuning pipeline for RWKV on story data

---

## G. Progress Log (Append Only)

```markdown
## 2026-07-21
- Removed `crates/tui/` (dead stub), `apps/{chat,studio,editor}/` (deprecated web apps), 16 probe/eval example binaries
- Fixed 2 flaky/broken tests: grammar FIM random walk (max_tokens 128→256), app facade workspace timeline (unique workspace name per test run)
- All 400+ workspace tests pass
- Updated docs: README, PROJECT_STRUCTURE, EDIT_GUIDE, AGENTS.md v4.0
- Created this v1.md roadmap
```