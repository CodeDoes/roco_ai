# Use hoisted node-linker so top-level node_modules + .bin symlinks
# are created (needed for `next` CLI + Next.js build/dev).
node-linker=hoisted

# Retry slow registry downloads (this env has ~10-30 KiB/s tars).
fetch-retries=5
fetch-retry-maxtimeout=600000
fetch-timeout=600000
