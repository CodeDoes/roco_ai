//! RoCo AI — gateway server (Phase 3).
//!
//! An axum HTTP server that exposes an RPC endpoint (SSE/WS) for remote
//! clients. Streams `TraceEvent`s and accepts task submissions.
//!
//! Stub: not yet implemented.

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    eprintln!("roco-gateway: not yet implemented (Phase 3)");
    Ok(())
}
