[package]
name = "roco-agent"
version.workspace = true
edition.workspace = true

[lib]
name = "roco_agent"

[dependencies]
roco-agent-core = { path = "../agent-core" }
roco-agent-story = { path = "../agent-story" }
