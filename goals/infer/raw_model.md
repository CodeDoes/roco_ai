# Raw Model

Intent: Load and run an unquantized RWKV-7 SafeTensors model as the foundational, correct-reference inference path before any optimization is layered on.

User: Can check out the newer g1h variant

Can also check out the 1.5b instead.
