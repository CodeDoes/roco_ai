# Devloop

Intent: The agent provisions and runs its own develop → test → lint loop inside a central, controlled location. All changes stay within a tracked, reversible workspace.

User: The agent sets up its own devloop — code execution, testing, and linting all happen within a central location.
