# Package Allowlist

Intent: Restrict dependencies to a human-approved allow-list (pre-approved or approved on demand), tying into `human_approval` before any new package is installed.

User: The only packages allowed might be approved by the human beforehand or within an allow list.
