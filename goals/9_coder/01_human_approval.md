# Human Approval

Intent: Gate high-impact coder actions (installing packages, executing code) behind explicit human confirmation or an approved allow-list. This is the gate that lets the devloop run.

User: Enables the devloop — the agent can't exec/install without it. In the current project the agent never execs, so this goal is future-only.
